package io.github.agenttroll.ptr.protocol;

// Represents an abstract superclass for all
// incoming messages. They must decode the components
// given by the constructor of this class
public class InMsg {
    public InMsg(String[] components) {
    }
}
